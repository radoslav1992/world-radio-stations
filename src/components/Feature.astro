---
interface Props {
  title: string;
  description: string;
  icon: string;
}
const { title, description, icon } = Astro.props;
---
<div class="feature">
  <div class="icon" set:html={icon}></div>
  <h3>{title}</h3>
  <p>{description}</p>
</div>
